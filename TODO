Should I put a tracker for using test() without a module() in deno, node.js and maybe even browser?

Enforce assert.timeout(ms): currently validates the argument but does not actually abort the test after N ms

Add assert.match() / assert.notMatch() for general purpose object pattern matching (strings, instances, objects, arrays, etc.)
